We assume that you have installed intel vtune in $HOME/intel/oneapi/vtune directory. 
In case you installed vtune in another directory (i.e, root mode), you must update the path for vtune in Make/options file.
Then type wmake to compile this solver.